<!DOCTYPE html>
<html>

<head>
    <meta charset="utf-8">
    <title>All YOUR VIDEOS</title>
    <style>
        body {
            font-family: arial;
            margin: 0;
        }
        
        nav {
            background: #ff5353;
            padding: 20px 10px;
        }
        
        nav ul {
            max-width: 960px;
            margin: 0 auto;
            padding: 0;
        }
        
        nav li {
            list-style-type: none;
            display: inline-block;
            margin: 0 10px 0 0;
        }
        
        nav a {
            color: #fff;
            font-size: 18px;
            background: rgba(255, 255, 255, 0.2);
            text-decoration: none;
            padding: 10px;
            display: block;
        }
        
        main,
        header {
            max-width: 960px;
            margin: 30px auto;
            padding: 0 10px;
            color: #333;
        }
        
        main img {
            width: 40px;
            margin-right: 10px;
            position: relative;
            top: 12px;
        }
        
        main p {
            display: inline-block;
        }
    </style>
</head>

<body>
    <nav>
        <ul>
            <% if (user) { %>
                <li><a href="/auth/logout">Log out</a></li>
                <% } else { %>
                    <li><a href="/auth/login">Login</a></li>
                    <% } %>
                        <li><a href="/">Homepage</a></li>
        </ul>
    </nav>
    <header>

        <h1>
            <%= user.name %> 's uploaded videos:
        </h1>

    </header>
    <main>
        <ul>
            <% for(var i=0; i<data.length; i++) { %>
                <li>
                    <%= data[i].snippet.title %><br/> description:
                        <%= data[i].snippet.description %><br/> publish date:
                            <%= data[i].snippet.videoId %><br/>
                                <p>uploads id:</p>

                </li>
                <% } %>
        </ul>
        <ul>
        <% if (comments.length) { %>
        <% for(var i=0; i<comments.length; i++) { %>
        
        <li>
           <h3><%= comments[i].author %></h3>
           <p><%= comments[i].comment %></p>

        </li>

        <% } %>


            <% } else { %>
            <p>no comments</p>
        <% } %>
            
        </ul>
    </main>
</body>

</html>